# Core libraries pinned to the versions used for the reference run
transformers==5.13.1
datasets==5.0.0
kagglehub==1.0.2
scikit-learn==1.6.1

# PyTorch: install the build that matches your CUDA driver.
# Reference run used torch 2.10.0+cu128 on a Tesla T4 (Google Colab).
# GPU users: pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.4.0

# Data handling, plotting and model persistence
numpy>=1.26
pandas>=2.1
matplotlib>=3.8
joblib>=1.3

# Notebook runtime (not required if running in Colab or Kaggle)
jupyter>=1.0
ipykernel>=6.29
ipywidgets>=8.1
